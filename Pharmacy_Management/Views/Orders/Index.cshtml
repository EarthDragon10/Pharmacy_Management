@model IEnumerable<Pharmacy_Management.Models.Orders>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Medicines.DescriptionUse)
        </th>
        <th>
            Autorizzazioni
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Quantity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateOrder)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TotalPrice)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Medicines.DescriptionUse)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pescritions.IdentifierPrescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Quantity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateOrder)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TotalPrice)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdOrder }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdOrder }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdOrder })
        </td>
    </tr>
}

</table>
