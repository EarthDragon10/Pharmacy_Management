@model Pharmacy_Management.Models.TypeProduct

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="vh-100 px-3 py-5">
    <h1><span class="text-grey">Admin</span> > Cancellazione Tipologia di Prodotto</h1>
    
    <h3 class="text-danger">Sei Sicuro di volerlo cancellare?</h3>
    <div>


        <div class="row mt-4">
            <div class="col">
                <div class="card">
                    <h5 class="card-header text-center">Tipologia Prodotto</h5>
                    <div class="card-body d-flex justify-content-around text-center">
                        <div>
                            <h5 class="card-title">@Model.DescTypeProduct</h5>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

    <div class="form-actions no-color">
        <input type="submit" value="Cancella" class="btn btn-success" />
        @Html.ActionLink("Torna Indietro", "Index", null, new { @class = "btn btn-primary" })
    </div>
        }
    </div>

</div>
