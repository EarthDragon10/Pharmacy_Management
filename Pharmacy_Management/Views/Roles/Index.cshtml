@model IEnumerable<Pharmacy_Management.Models.Roles>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TypeRole)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TypeRole)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdRole }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdRole }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdRole })
        </td>
    </tr>
}

</table>
